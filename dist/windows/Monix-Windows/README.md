# Monix Windows Package

Run `MonixInstaller.exe`.

Keep the `payload` folder next to the installer. The installer copies Monix, its visual assets, sounds, configuration, font and collector files, then creates Desktop and Start Menu shortcuts.

If launched as administrator, Monix installs under Program Files. Otherwise it installs for the current user under Local AppData.
